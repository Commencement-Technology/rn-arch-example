import React from "react"
import { View, StyleSheet } from "react-native"

type AuthProps = {
}

function Auth({ }: AuthProps): React.JSX.Element {
    return (
        <View
            style={styles.container}>
        </View>
    )
}

const styles = StyleSheet.create({
    container: {
    },
})

export default Auth